/**
 * Represents the structured response for web search results selection
 */
export interface WebSearchResponse {
    /**
     * Array of selected URLs most relevant to the research goal
     */
    urls: string[];
}
